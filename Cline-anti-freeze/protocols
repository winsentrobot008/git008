# ============================================
# Cline-anti-freeze 治理核心协议声明
# 版本: 2.0 — 多实例并行协作协议
# ============================================
#
# 本文件夹为 git008 项目唯一的系统治理中心。
# 职能范围：
#   1. 全局治理规则定义与分发
#   2. 防卡死（Anti-Freeze）强制协议执行
#   3. 错误日志记录与监控
#   4. 各业务项目的治理链接管理
#   5. 多实例并行协作治理（工位隔离、写入互斥、心跳同步）
#
# 所有 Cline 实例必须：
#   - 启动时通过 governance_linker.py --boot-check 验证治理中心
#   - 遵守防卡死约束（超时熔断、循环检测、上下文保护、心跳检查、异常退出）
#   - 遵守多实例并行协作协议（工位职能隔离、并行写入互斥、宪法唯一源、心跳同步）
#   - 通过 governance_linker.py 与各项目建立治理链接
#
# 工位职能定义：
#   - 治理工位 (governance)：唯可修改宪法核心文件的角色，负责运行 monitor.py --daemon
#   - 开发工位 (development)：仅可编码业务逻辑，长任务需发送心跳存活信号
#
# 本协议自加载起永久生效。