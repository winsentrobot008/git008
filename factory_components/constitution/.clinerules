# git008 根级治理规则 — 仅作为治理中心引用的入口
# 所有治理规则统一存放于 Cline-anti-freeze/

include: ../tools/Cline-anti-freeze/.clinerules

# ==========================================
# 🛡️ SECTION 6: 核心资产操作白名单 (Execution Whitelist)
# ==========================================
# 本白名单由 Constitution Article 2.2 / 5.4 / 5.6 授权执行
# 任何 Agent 进程违反本条红线将被直接拦截并中止任务

## 1. 白名单准入路径 (Authorized Target Paths)
- `/factory_components/second_brain/`       ── [第二大脑记忆中枢]
- `/factory_components/vision_engine/`      ── [上游视觉感知动力源]

## 2. 授权操作行为 (Permitted Operations - ONLY)
- [READ]   允许对上述目录下的 Markdown、Python、Images 进行读取、扫描与解析。
- [WRITE]  允许 `knowledge_linker.py` 在 `wiki/` 目录内新建、追加或修改 `.md` 知识笔记与哈希记录。
- [EXECUTE] 允许在终端触发 `.governance_entry.py` 及相关心跳维护脚本。

## 3. 绝对禁止红线 (Strictly Prohibited - BREAK-ON-FAIL)
- ❌ 严禁使用任何形式的目录级删除、剪切、清空命令
   （包括但不限于 `shutil.rmtree`、`os.remove`、`Path.unlink`、`os.system('rm -rf')`）。
- ❌ 未经 CEO 显式授权，严禁擅自修改两项资产的物理拓扑结构与子文件夹命名规则。
- ❌ 违反本条红线的任何 Agent 进程，系统将直接执行强力拦截并中止当前任务。

### §3.4 文件读取计数熔断硬限（防闪退核心）
- 单次读取上限硬性限制为 100 个条目。
- 探测到路径包含 node_modules、.git、dist、build、__pycache__ 等垃圾场，必须立刻无条件强行终止（Abort）并报错。
- 严禁对中大型或未知大小目录调用高级智能体文件读取工具，必须降级使用终端底层命令（如 dir 或 ls）。

### §3.5 嵌套 Git 与检查点死锁防御
- 嵌套 Git 环境下主动判定检查点机制失效，1 秒内无条件跳过快照过程，严禁引发任何后台挂起等待。

### §3.6 绝对路径锁定与边界防御
- 进入任何子项目开发的第一步必须先在终端执行 `cd <项目名>`，后续所有操作死锚在内，严禁进行发散式全盘越界扫描。

### §3.7 未知代码文件解剖与增量修改硬限（防写过载）
- **体积前置前瞻**：严禁对任何超过 300 行或体积未知的代码文件调用全量文件读取工具。必须降级使用终端轻量流命令先获取总行数。
- **切片式采样**：对于中大型文件，必须采取"只看骨架"的切片式读取，单次读取行数上限硬性限制为 50-60 行。
- **原子化增量修改**：修改代码时，严禁重写整个文件。必须使用精细替换工具进行原子化增量修改，且修改后必须使用终端编译命令（如 `python -m py_compile`）进行轻量验证，失败则原地熔断。
- **轻量编译熔断**：任何写操作完成后，严禁盲目直接运行业务。必须在终端先执行轻量编译验证（如 `python -m py_compile`），若编译失败（Exit code != 0），必须立刻原地熔断并报错修复，禁止推进流水线。
### §3.4 文件读取计数熔断硬限（防闪退核心）
- 单次读取上限硬性限制为 100 个条目。
- 探测到路径包含 node_modules、.git、dist、build、__pycache__ 等垃圾场，必须立刻无条件强行终止（Abort）并报错。
- 严禁对中大型或未知大小目录调用高级智能体文件读取工具，必须降级使用终端底层命令（如 dir 或 ls）。

### §3.5 嵌套 Git 与检查点死锁防御
- 嵌套 Git 环境下主动判定检查点机制失效，1 秒内无条件跳过快照过程，严禁引发任何后台挂起等待。

### §3.6 绝对路径锁定与边界防御
- 进入任何子项目开发的第一步必须先在终端执行 `cd <项目名>`，后续所有操作死锚在内，严禁进行发散式全盘越界扫描。

### §3.7 未知代码文件解剖与增量修改硬限（防写过载）
- 严禁对任何超过 300 行或体积未知的代码文件调用全量文件读取工具。必须降级使用终端轻量流命令先获取总行数。
- 修改代码时严禁重写整个文件，必须使用精细替换工具进行原子化增量修改，且修改后必须使用终端编译命令（如 python -m py_compile）进行轻量验证，失败则原地熔断。
